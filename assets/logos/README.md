# Company Logo

Place `logo.png` in this folder to embed the company logo in generated PDF invoices.

## Requirements

- **Filename:** `logo.png`
- **Format:** PNG (transparent background preferred)
- **Recommended size:** ~300 × 80 px (wide format works best in the PDF header)

If `logo.png` is missing, the header will show the company name as text only.
